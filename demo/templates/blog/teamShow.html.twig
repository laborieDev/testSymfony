{% extends 'base.html.twig' %}

{% block body %}

<h1>Bienvenue dans l'équipe</h1>
<h2>Voici nos champions : </h2>
<section>
    <div>
        <h3> ----- team n°{{ team.id }} ----- </h3> 
        <p>Nom : {{ team.name }}</p>
        <p>Entreprise : {{ team.company }}</p>
        <p>Salaire : {{ team.salary }}</p>
        <a href="{{ path('equipe_edit' , {'id':team.id}) }}" class="btn btn-primary">Modifier</a>
    <div>
</section>

{% endblock %}